# Shravana GPU Worker — dependencies
# Install with: pip install -r worker/requirements.txt
#
# This is a SUBSET of backend/pyproject.toml. Install from the backend first:
#   pip install -e backend/
# then install any worker-specific extras below.

# Server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Audio
soundfile>=0.12.1
librosa>=0.10.1
noisereduce>=3.0.0

# ML — install torch separately (GPU build recommended):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0
torchaudio>=2.2.0
torchvision>=0.17.0
transformers>=4.40.0
accelerate>=0.27.0
qwen-asr>=0.0.6
huggingface-hub>=0.22.0
faster-whisper>=1.0.0
demucs>=4.0.0

# Utils
tqdm>=4.65.0,<5.0.0
aiofiles>=23.2.1
